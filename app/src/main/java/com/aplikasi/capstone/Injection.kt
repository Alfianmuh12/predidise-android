package com.aplikasi.capstone

import android.content.Context

object Injection {
    //fun provideRepository(context: Context?): TourismRepository {
    //    val database = context?.let { TourismDatabase.getDatabase(it) }
    //val apiService = ApiConfig.getApiService()

        //return TourismRepository(database, apiService)
    //}
}